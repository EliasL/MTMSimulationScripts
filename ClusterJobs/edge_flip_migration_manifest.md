# Edge-flip migration manifest

Read-only snapshot taken 2026-08-07. No jobs, folders, or files were changed.

## Expected campaign

- Cluster configurations: 74
- Local configurations: 8
- Current expected folder names are the names generated by the corrected Python defaults.

## Cluster audit

All 74 expected configuration names were found at least once. There are 86 physical
folders because 12 implicit-default configurations are duplicated on two servers.

| Effective settings from `config.conf` | Physical folders | Action |
| --- | ---: | --- |
| Explicit `reconnectRevert=0`, `reconnectEdgeLocking=1` | 50 | Rename to the canonical legacy name containing both settings |
| Both keys absent; remote source defaults to `1,0` | 36 (24 unique configurations) | Already uses the desired effective settings; leave in place and resume |

The 12 duplicate implicit-default folders are on Galois/Descartes (batches 1–2)
and Schwartz/Descartes (batch 4). No legacy `(0,1)` destination-name collisions
were found on any run server.

Explicit legacy folders by server:

- Pascal: 18
- Fourier: 8
- Duchemin: 12
- Dalembert: 5
- Legendre: 5
- Condorcet: 1
- Poincare: 1

## Local audit

All 8 local folders exist:

- 7 explicit legacy `(0,1)` folders: batch 5 (4) and batch 8 (3)
- 1 hybrid `(1,1)` folder: batch 8, seed 3

No local legacy destination-name collisions were found. The hybrid folder should
be preserved under a distinct name containing `reconnectEdgeLocking1`, then batch
8 seed 3 can be restarted with the corrected `(1,0)` settings.

## Migration rules

For explicit legacy folders, rename the directory to the name generated with
`reconnectRevert=0, reconnectEdgeLocking=1`, and update the `name = ...` line in
the contained `config.conf`. Keep the filename `config.conf` and all dumps/data
files unchanged.
